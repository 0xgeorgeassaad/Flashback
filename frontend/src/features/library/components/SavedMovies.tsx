import { TodoPanel } from '../../../components/ui/TodoPanel'

export function SavedMovies() {
  return (
    <TodoPanel owner="Contributor 6" title="Saved and watched movies">
      TODO: render saved movies with watched toggles, remove/undo behavior, details links, responsive
      card/list layouts, and an empty state that leads users back to Discover.
    </TodoPanel>
  )
}
