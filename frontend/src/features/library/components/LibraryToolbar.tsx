import { TodoPanel } from '../../../components/ui/TodoPanel'

export function LibraryToolbar() {
  return (
    <TodoPanel owner="Contributor 6" title="Organize my list">
      TODO: All/Saved/Watched filters, title search, sort by saved date/title, result count, and a
      carefully confirmed Clear local data action.
    </TodoPanel>
  )
}
