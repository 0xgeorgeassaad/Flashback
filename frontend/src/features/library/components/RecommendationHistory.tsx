import { TodoPanel } from '../../../components/ui/TodoPanel'

export function RecommendationHistory() {
  return (
    <TodoPanel owner="Contributor 6" title="Recent recommendation sessions">
      TODO: group locally stored sessions by date, reopen a session, remove one session, handle stale
      catalog entries, and explain that this history exists only in this browser.
    </TodoPanel>
  )
}
