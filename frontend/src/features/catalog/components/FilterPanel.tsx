import { TodoPanel } from '../../../components/ui/TodoPanel'

export function FilterPanel() {
  return (
    <TodoPanel owner="Contributor 2" title="Filter and sort">
      TODO: genre and decade controls, active-filter chips, clear-all action, title/year sort, grid/list
      switch, URL query-parameter synchronization, and responsive mobile disclosure.
    </TodoPanel>
  )
}
