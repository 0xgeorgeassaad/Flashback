import { TodoPanel } from '../../../components/ui/TodoPanel'

export function MovieSearch() {
  return (
    <TodoPanel owner="Contributor 2" title="Search the archive">
      TODO: controlled search input, result count, matched-text highlighting, clear action, keyboard
      behavior, and a useful no-results state. Search the loaded catalog locally.
    </TodoPanel>
  )
}
