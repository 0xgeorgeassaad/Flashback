import { TodoPanel } from '../../../components/ui/TodoPanel'

export function SelectedMovieList() {
  return (
    <TodoPanel owner="Contributor 4" title="Selected movies">
      TODO: render selected cards, remove and undo actions, prevent duplicates, preserve focus after
      removal, and design an inviting empty state that links back to Discover.
    </TodoPanel>
  )
}
