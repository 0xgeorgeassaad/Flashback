import { TodoPanel } from '../../../components/ui/TodoPanel'

export function TasteSummary() {
  return (
    <TodoPanel owner="Contributor 4" title="Taste summary and confirmation">
      TODO: calculate genre counts from the selected movies, show the dominant mix honestly, validate
      the five-title minimum, call the recommendation hook, and navigate to Results on success.
    </TodoPanel>
  )
}
