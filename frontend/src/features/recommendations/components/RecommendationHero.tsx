import { TodoPanel } from '../../../components/ui/TodoPanel'

export function RecommendationHero() {
  return (
    <TodoPanel owner="Contributor 5" title="The top pick">
      TODO: feature the first recommendation with strong poster-led hierarchy, score/rank treatment,
      Save action, responsive layout, and a clear fallback when no results exist.
    </TodoPanel>
  )
}
