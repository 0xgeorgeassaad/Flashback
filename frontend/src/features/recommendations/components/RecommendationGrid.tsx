import { TodoPanel } from '../../../components/ui/TodoPanel'

export function RecommendationGrid() {
  return (
    <TodoPanel owner="Contributor 5" title="The remaining recommendations">
      TODO: reuse Contributor 3's card primitives, show recommendation rank and score, support Save and
      Details actions, and include loading skeleton, error/retry, and partial-response states.
    </TodoPanel>
  )
}
