# The deployed FastAPI backend is the default when this variable is omitted.
# Change this to http://localhost:8000 only when running the backend locally.
VITE_API_URL=https://flashback.fastapicloud.dev
